{
  "name": "order-ws",
  "version": "1.0.0",
  "description": "WebSocket server for orders",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://git.heroku.com/order-ws.git"
  },
  "author": "niklissad",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.0"
  }
}
